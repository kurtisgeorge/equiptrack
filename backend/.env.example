# Turso (libSQL) database connection. Create a free database at
# https://turso.tech, then copy its URL and an auth token here.
TURSO_DATABASE_URL=libsql://your-database-org.turso.io
TURSO_AUTH_TOKEN=

# Optional. Local dev / `npm start` default to 4000 if unset.
PORT=4000
